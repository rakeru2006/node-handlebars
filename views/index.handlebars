<h1>Cats_Burgers!</h1>

<h2>Cats_Burgers that are not sleepy_devour it!</h2>

<ul>
  {{#each cats}}
    {{#unless sleepy}}
      {{> cats/cat-block sleep=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>cats_burger that are sleepy_well!</h2>

<ul>
  {{#each cats}}
    {{#if sleepy}}
      {{> cats/cat-block sleep=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a Cat_burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Cat_burger Name:</label>
    <input type="text" id="ca" name="name">
  </div>

  <div class="form-group">
    <label for="sleepy">Sleepy_devorit?</label><br>
    <input type="radio" name="sleepy" value="1" checked> Sleepy DEVOUR IT AGAIN !<br>
    <input type="radio" name="sleepy" value="0"> Awake_NEW BURGER
  </div>

  <button type="submit">Add Cat_burger</button>
</form>
