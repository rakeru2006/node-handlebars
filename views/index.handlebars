

<h1>Burgers!</h1>
<div class="myDiv1"></div>
<div class="myDiv2"></div>
<div class="myDiv3"></div>
<div class="myDiv4"></div>
<div class="myDiv5"></div>

<h2>Burgers that are devoured it already!</h2>

<ul class="a">
  {{#each tb_burgers}}
    {{#unless devoured}}
      {{> burger/burger-block devoured=true}}
    {{/unless}}
  {{/each}}
</ul>



<h2> New Burgers Menu to enjoy !</h2>

<ul class="b">
  {{#each tb_burgers}}
    {{#if devoured}}
      {{> burger/burger-block devoured=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a Your Special Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="burger_name">Burger Name:</label>
    <input type="text" id="burger_name" name="burger_name">
  </div>

  <div class="form-group">
    <label for="devoured">devoured?</label><br>
    <input type="radio" name="devoured" value="1" checked> NEW BURGER !<br>
  </div>

  <button type="submit">Save New_burger</button>
</form>
